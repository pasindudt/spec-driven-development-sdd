# Specs (source of truth)

This directory contains the *specifications* for the project.

- `product.md` 
 Product goals, user stories, non-goals
- `site.yml` 
 Information architecture + content requirements
- `ui.yml` 
 UI behavior + interactive components

Any change to behavior should begin with a change here.
